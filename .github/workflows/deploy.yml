name: 🚀 Deploy Muorg (VPS)

on:
  workflow_dispatch:
  push:
    branches: ['main']
    tags: ['v*']

concurrency:
  group: deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: Production
      url: http://${{ secrets.SSH_HOST }}:7800
    steps:
      # Steps are guarded on the secrets being present so the workflow
      # skips cleanly until SSH_HOST/USERNAME/KEY are added to this repo.
      - name: Get Known Hosts value
        if: ${{ secrets.SSH_HOST != '' }}
        run: echo "KNOWN_HOSTS=$(ssh-keyscan -H ${{ secrets.SSH_HOST }})" >> $GITHUB_ENV

      - name: Install SSH Key
        if: ${{ secrets.SSH_KEY != '' }}
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_KEY }}
          name: github-actions
          known_hosts: ${{ env.KNOWN_HOSTS }}
          if_key_exists: replace

      - name: Pull and start docker images on VM
        if: ${{ secrets.SSH_HOST != '' && secrets.SSH_KEY != '' }}
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /root/code/muorg
            docker compose pull
            docker compose up -d
            docker image prune -f
            curl -sf http://localhost:7700/api/health > /dev/null && echo "✅ muorg-server healthy" || echo "⚠️ server health check failed"
            curl -sf -o /dev/null http://localhost:7800 && echo "✅ muorg-web up" || echo "⚠️ web check failed"
