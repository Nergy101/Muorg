This folder contains layout/shell components that structure the main UI:

- Sidebar, headers, and table containers
- Cross-cutting UI chrome that isn't specific to library, metadata, or playback features

